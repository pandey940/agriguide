# Set VITE_API_URL in Netlify → Site settings → Environment variables
# Example: https://agriguide-api.onrender.com/api
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
